# TEST_CASES.md — Test Catalog

> Test catalog for the boilerplate itself. When used to start a new project, replace this file with the project's own test cases.

---

## Boilerplate Smoke Tests

| ID        | Description                                                         | Status      |
| --------- | ------------------------------------------------------------------- | ----------- |
| SMOKE-001 | All required files exist (CLAUDE.md, HANDOFF.md, PROGRESS.md, etc.) | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-002 | package.json parses and scripts are valid                           | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-003 | tsconfig.base.json is valid JSON                                    | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-004 | turbo.json is valid JSON                                            | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-005 | pnpm-workspace.yaml is valid YAML                                   | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-006 | .editorconfig is valid                                              | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-007 | .prettierrc is valid JSON                                           | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-008 | .env.example has no secrets                                         | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-009 | .githooks/pre-commit is executable                                  | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-010 | .claude/settings.json is valid JSON                                 | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-011 | eslint.config.js is valid                                           | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-012 | All docs/ files have no broken internal links                       | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-013 | Doc freshness marker — **narrowed from the original spec**: HANDOFF.md carries no date field, and in Precast itself both docs ship as static templates, so a live 14-day gate would fail permanently in the repo it protects. Asserts PROGRESS.md's `Last Updated` exists and parses; the 14-day window is enforced only under `PRECAST_ENFORCE_DOC_FRESHNESS=1` (meaningful in a derived project)                               | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-014 | PROGRESS.md has no placeholder content                              | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-015 | All workspace packages have valid package.json                      | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| SMOKE-016 | `pnpm smoke:chat` — the END-TO-END chat round trip: a real message through the running agents API to a real model and back. Script, not a CI test, because it needs a running container and a working provider key; fails distinctly on "no LLM key" vs "container unreachable" vs "answered with no text" | **Not Started** — tool delivered and its failure paths verified, but the round trip itself has never been run (no provider key available). The largest unproven claim in the boilerplate |
| SMOKE-017 | A fresh scaffold can actually BOOT: `bootstrap.mjs` mints an `AGENT_API_TOKEN` with a CSPRNG **and calls the generator** (asserting the function merely exists is not enough — deleting only the call site reproduces the original bug with a green suite, which mutation-testing caught); `.env.example` still ships the token EMPTY so the committed file carries no secret; and every committed placeholder host is recognisably fake, so truthiness checks cannot mistake one for configuration | Passing (4 cases) |
| SMOKE-018 | `AGENTS.md` stays true to the repo: it points at CLAUDE.md, every doc it links to resolves, every `pnpm` command it names is a real script, every invariant it asserts is still asserted in CLAUDE.md, and it hardcodes no ports (`set-ports.mjs` rewrites CLAUDE.md/SPEC.md but NOT this file, so a port here would be wrong the moment a project is scaffolded). Asserts the CLAIMS, not the file's existence — a pointer file rots otherwise. Mutation-verified 4 ways | Passing (5 cases) |

---

## Application Smoke Tests

> Verified manually on 2026-07-06 (Node 25.2.1 / pnpm 9.15.9). Automate under CI when it moves in scope.

| ID        | Description                                                                                                                                                                                                                                                                                                            | Status                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| APP-001   | `pnpm build` succeeds for shared + agents (mastra build) + web from clean                                                                                                                                                                                                                                              | Passing                                                                                                                                                                                                                                                                                                                                                                                                                  |
| APP-002   | `pnpm typecheck` passes across all packages                                                                                                                                                                                                                                                                            | Passing                                                                                                                                                                                                                                                                                                                                                                                                                  |
| APP-003   | `pnpm lint` passes across all packages                                                                                                                                                                                                                                                                                 | Passing                                                                                                                                                                                                                                                                                                                                                                                                                  |
| APP-004   | `pnpm test` passes (agents + web Vitest)                                                                                                                                                                                                                                                                               | Passing                                                                                                                                                                                                                                                                                                                                                                                                                  |
| APP-005   | Mastra API boots; `GET /api/agents` lists the example agent                                                                                                                                                                                                                                                            | Passing                                                                                                                                                                                                                                                                                                                                                                                                                  |
| APP-006   | Web builds; `GET /api/health` (Next.js route handler) returns `{status:"ok"}`; `/` renders                                                                                                                                                                                                                             | Passing                                                                                                                                                                                                                                                                                                                                                                                                                  |
| APP-007   | Env validation fails fast when `DATABASE_URL` is missing/invalid                                                                                                                                                                                                                                                       | Passing (covered by web Vitest)                                                                                                                                                                                                                                                                                                                                                                                          |
| APP-008   | Compose publishes host ports from `AGENTS_HOST_PORT`/`WEB_HOST_PORT`/`KEYCLOAK_HOST_PORT` (default = app port; Keycloak's container port is always 8080; override remaps host side only)                                                                                                                               | Passing (verified via `node scripts/docker-compose.mjs -f docker/docker-compose.yml config` against a real root `.env` — the actual `pnpm docker:up` path: defaults 45000/45001/45002→8080; overriding in `.env` remaps host side only)                                                                                                                                                                                  |
| APP-009   | Compose includes a `keycloak` service (dev `start-dev`, published on `KEYCLOAK_HOST_PORT`); Postgres/Redis remain remote                                                                                                                                                                                               | Passing (verified via `docker compose config`: services = agents, keycloak, web; compose valid)                                                                                                                                                                                                                                                                                                                          |
| APP-010   | `pnpm docker:*` reads the root `.env` for Compose interpolation (fails fast with a clear message if `.env` is missing, via `scripts/docker-compose.mjs`); `COMPOSE_PROFILES` in `.env` selects which of agents/web/keycloak build+run (default = all three; e.g. `web,keycloak` excludes agents with no Compose error) | Passing (verified end-to-end: missing `.env` → exit 1 + message; default profiles → all 3 services; `COMPOSE_PROFILES=web,keycloak` → agents excluded cleanly; full config incl. override validates)                                                                                                                                                                                                                     |
| APP-011   | The stack owns ONE consecutive port block in stack order (agents → web → keycloak), inside 45000–49151 (above common dev defaults, below the OS ephemeral range); published host ports and host-facing URLs follow it; `pickConsecutivePorts()` only returns free, in-range, consecutive blocks                        | Passing (`apps/web/test/ports.spec.ts`, 8 cases)                                                                                                                                                                                                                                                                                                                                                                         |
| APP-012   | Every stage of every app Dockerfile builds on `node:24-alpine` (builder **and** runtime), and `.nvmrc` agrees with that major                                                                                                                                                                                          | Passing (`apps/web/test/docker-build.spec.ts`)                                                                                                                                                                                                                                                                                                                                                                           |
| APP-013   | Compose declares an explicit top-level `name:` so each Precast repo gets its own project namespace, and declares volumes **bare** (no project-name prefix — Compose adds it)                                                                                                                                           | Passing (`apps/web/test/docker-compose-profiles.spec.ts`) — regression: without the `name:` Compose derives the project from the `docker/` dir, and bringing this stack up **recreated another Precast project's Keycloak container**. Resolved names verified for a renamed project: project `slacker`, containers `slacker-{agents,web,keycloak}`, network `slacker_default`, volumes `slacker_{agents,keycloak}-data` |
| APP-014   | `pnpm docker:check` / `pnpm docker:wait` exist as runtime preflights (not Compose calls) and are invoked before any Compose command, so a missing/stopped engine yields install instructions rather than a daemon-socket error                                                                                         | Passing (script-shape guard in `docker-compose-profiles.spec.ts`; `pnpm docker:check` exercised live → engine reachable, exit 0)                                                                                                                                                                                                                                                                                         |
| APP-015   | No app Dockerfile bakes a non-Postgres `DATABASE_URL`/`MASTRA_DB_URL`. Compose sets neither key, so an image-layer `ENV` is what the container gets — and ADR-002's schema rejects anything but Postgres, making it a boot failure rather than a fallback                                                              | Passing (`apps/web/test/docker-build.spec.ts`) — regression: the agents image kept `file:/data/mastra.db` after v0.3.0 removed the Compose override. Negative-tested                                                                                                                                                                                                                                                     |
| APP-016   | The dependency base image stays optional: both app Dockerfiles declare `ARG BASE_IMAGE` defaulting to `node:24-alpine`, build `FROM ${BASE_IMAGE}`, and pass the same `--store-dir` that `deps.Dockerfile` bakes; `deps.Dockerfile` is itself `node:24-alpine` and copies only lockfile/manifest inputs                | Passing (`apps/web/test/deps-image.spec.ts`, 8 cases)                                                                                                                                                                                                                                                                                                                                                                    |
| APP-017   | `pnpm rename` leaves `ghcr.io/...` references verbatim while still renaming every case form around them — the Dockerfiles and compose file are rename targets, so an unprotected registry ref would 404 on the first scaffolded build                                                                                  | Passing (`apps/web/test/deps-image.spec.ts`, 4 cases)                                                                                                                                                                                                                                                                                                                                                                    |
| APP-021   | `pnpm rename` leaves the `@precast/*` workspace scope untouched (imports, tsconfig aliases, `pnpm -F` filters) while still renaming runtime identity — Compose project `name:`, `container_name:`, Keycloak client id. Both halves asserted: the scope is internal and cannot collide, but runtime names DO (APP-013)  | Passing (`apps/web/test/deps-image.spec.ts`, 2 cases)                                                                                                                                                                                                                                                                                                                                                                    |
| APP-022 | Both runtime images **drop root**: each Dockerfile declares `USER node`, places it AFTER the final `COPY` (switching earlier copies into a root-owned WORKDIR), and copies with `--chown=node:node` — without which the bundle stays root-owned, still readable, so the app boots and the gap only appears when something needs to write. Verified by RUNNING both images: each reports `uid=1000(node)`, the agents card answers 200 and the web app renders 200. Mutation-verified 3 ways | Passing (6 cases) |
| APP-018   | The deps-image cache key is a stable `lock-<16 hex>` derived from the lockfile + dependency blocks only — never package names, so a renamed project keeps the cache                                                                                                                                                    | Passing (`apps/web/test/deps-image.spec.ts`, 4 cases)                                                                                                                                                                                                                                                                                                                                                                    |
| APP-019   | `.env.example` never lets an inline `#` comment become part of a value, and ships no non-ASCII inside a value                                                                                                                                                                                                          | Passing (`apps/web/test/ports.spec.ts`, 2 cases) — regression: `GOOGLE_GENERATIVE_AI_API_KEY=      # or GOOGLE_API_KEY — either is accepted` shipped a 48-char "key" containing an em dash to every scaffold; Compose's `env_file` does not strip inline comments. It silenced the no-provider warning AND broke every LLM call in header encoding. Negative-tested                                                      |
| APP-020   | `bootstrap.mjs` offers AgentBase Models, collects all five gateway settings (not just the credentials), exposes a `--agentbase-*` flag for each so CI works, writes them to `.env` and **never** to the committed `.env.example`, does not demand a vendor key for that choice, and **never writes the retired `AGENTBASE_LLM_*` credential names** — a second name for one credential would let an empty later assignment silently blank a real earlier one (verified: dotenv resolves `A=real` then `A=` to `""`)                                     | Passing (`apps/web/test/bootstrap-llm.spec.ts`, 6 cases — source-analysed, since importing bootstrap runs `main()` and wipes git)                                                                                                                                                                                                                                                                                        |
| SMOKE-016 | `pnpm poc` brings the whole Compose stack up on the assigned port block and every service answers: agents `GET /api/agents` lists the example agent, web `GET /api/health` + `/` return 200, Keycloak serves                                                                                                           | Passing (run live: agents 45000, web 45001, keycloak 45002 → all health-green; `curl /` → 200; `/api/agents` → example-agent)                                                                                                                                                                                                                                                                                            |
| SMOKE-017 | `pnpm poc <service>` reflects a code change in the running preview and re-prints the URLs, recreating **only** the named container                                                                                                                                                                                     | Passing (run live: edited `apps/web/app/page.tsx` heading → `pnpm poc web` → `curl /` served the new text, and only `precast-web` was recreated while `agents`/`keycloak` kept their uptime. First attempt without `--no-deps` also recreated `agents` via `depends_on` — fixed, then re-verified)                                                                                                                       |

---

## Shared Package Tests (Vitest, `packages/shared/src/`)

| ID      | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Status             |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| SHR-001 | `database.spec.ts` — `isPostgresUrl()` / `assertPostgresUrl()` (ADR-002, Postgres-only): accepts `postgres://`/`postgresql://`; **rejects** the engines ADR-001 used to allow (`file:`, `sqlite:`, `libsql:`) plus unrelated schemes (`mysql://`); `isPostgresUrl` returns `false` rather than throwing on malformed input so it is safe as a zod `.refine()`; `assertPostgresUrl` distinguishes "not a URL" from "wrong engine", names the offending scheme, points at ADR-002, and uses the caller-supplied variable name | Passing (13 cases) |

---

## Agent Tests (Vitest, `apps/agents/src/mastra/`)

> Neutral placeholder — replace with your agents' tests.

| ID      | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Status                                       |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| AGT-007 | `agentbase-mcp.spec.ts` — MCP discovery is **inert off AgentBase** (no network call, no throw, so local dev/Standalone/External are unaffected) and **throws when hosted and discovery fails**, rather than degrading to an empty toolset; tolerates a trailing slash on the injected base URL and treats a missing `mcpServers` key as an empty entitlement. **Connect** failures are partitioned (v0.8.1): a subscribed server answering 401 `mcp_server_authorization_required` is SKIPPED with a warning — that state clears only when a human connects an account, so failing the boot crash-loops the container — while any other connect failure still throws, and a healthy server's tools survive alongside a skipped one. Mutation-verified in both directions: a predicate that always matches fails the non-auth case, one that never matches fails the skip cases, and dropping the warning fails the first                                                                                                                                                                                                                                                                                                                                        | Passing (9 cases)                            |
| AGT-008 | Account-level AgentBase credentials (`--llm-provider=agentbase`): `isAgentBaseLlmConfigured()` is true only for a complete set; an unsuffixed `AGENTBASE_LLM_MODEL` drives the gateway when no per-agent one is injected; an injected **per-agent** model wins over the account-level one; and with nothing AgentBase-related set the plain fallback is returned untouched                                                                                                                                                                                                                                                                                                                          | Passing (`agentbase-model.spec.ts`, 5 cases) |
| AGT-009 | AgentBase credential naming is capability-neutral: `AGENTBASE_CLIENT_ID`/`_SECRET`/`AGENTBASE_TOKEN_URL` authenticate the LLM gateway, the MCP proxy and A2A alike. The legacy `AGENTBASE_LLM_*` credential names remain a **live** fallback because AgentBase injects them into hosted containers; resolution runs per-agent canonical → per-agent legacy → account canonical → account legacy, so an admin's Studio choice still outranks a repo `.env`. Each branch mutation-verified (removing a fallback, or flipping canonical/legacy priority, fails a distinct case) | Passing (`agentbase-model.spec.ts`, 6 cases) |
| AGT-010 | `auth.spec.ts` — the production boot guard: inert outside production; refuses to start in production with no token; **treats an EMPTY token as absent** (`.env.example` ships `AGENT_API_TOKEN=`, which arrives as `''` — the fresh-scaffold case that broke `pnpm poc`); starts once a token is set; honours an explicit `ALLOW_UNAUTHENTICATED_AGENT_API=1` opt-out and only that exact value, so `=false` cannot disable it | Passing (6 cases) |
| AGT-011 | `resolveLlmBaseUrl` derives the LLM gateway base from `AGENTBASE_URL` + `AGENTBASE_LLM_MODEL` via `agentbase.list_models` (the route is per-model — `/proxy/llm/<org>/<slug>/v1` — so it is not a prefix). An **injected** base always wins and the registry is never called: a hosted service application cannot use the native MCP tools at all, so a lookup there would break every import. Matches slug, `<provider>/<model-id>`, or a bare model id; caches; names the available models when nothing matches; treats the `.env.example` placeholder as unconfigured. Mutation-verified 4 ways | Passing (6 cases) |
| AGT-001 | `echo` returns its input message                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Passing                                      |
| AGT-002 | `agentbase-model.spec.ts` — `resolveAgentModel()`: **off** AgentBase (unset `AGENTBASE_HOSTED`) falls back to the plain router string + `.env` key; builds a gateway-backed model (keyed to `AGENTBASE_LLM_MODEL_<AGENT_ID>`) once both the base URL and this agent's model are set; **on** an AgentBase-hosted container (`AGENTBASE_HOSTED=1`) an unconfigured agent does NOT fall back to the env key — it returns a model that fails at CALL time with an actionable message. `getAgentBaseToken()` mints via OAuth2 `client_credentials`, caches, auto-refreshes near expiry, and errors clearly on missing config / non-2xx                                                                | Passing                                      |
| AGT-003 | `default-model.spec.ts` — `resolveDefaultModel()`: explicit `DEFAULT_LLM_MODEL` always wins (including a 3-segment router string, whose vendor prefix passes through intact); auto-detects each of the 11 supported providers independently from its own key (one row per provider); `GOOGLE_API_KEY` works as an alias for `GOOGLE_GENERATIVE_AI_API_KEY`; documented tie-break prefers the earliest-listed provider, and any direct provider over a router; returns a call-time-failing model (never a string) when nothing is configured, with a message naming every supported env var (asserted via a regex generated from `SUPPORTED_LLM_ENV_VARS`, so it can't drift as providers are added) | Passing                                      |
| AGT-004 | `default-model.spec.ts` — `isLlmProviderConfigured()`: false when nothing is set; true for `DEFAULT_LLM_MODEL`, independently for each provider key in `SUPPORTED_LLM_ENV_VARS`, and for the `GOOGLE_API_KEY` alias. Backs the boot-time warning in `mastra/index.ts` (`pnpm dev`/`mastra start` logs loudly if unconfigured, instead of only failing mid-conversation)                                                                                                                                                                                                                                                                                                                             | Passing                                      |
| AGT-005 | `lib/crew.spec.ts` — `defineCrew()`/`crewAgents()`: at 2+ members a distinct `crew-orchestrator` is built with each member wired as an in-process sub-agent (Agent Network); at N=1 it collapses to the sole member (no orchestrator, no extra card); guards reject an empty crew, a member squatting the reserved `crew-orchestrator` id, duplicate member ids, and a nested crew (a member that is itself an orchestrator). `crewAgents()` registers members + orchestrator de-duplicated when active, and just the sole member at N=1.                                                                                                                                                           | Passing                                      |
| AGT-006 | `orchestration-manifest.spec.ts` — the committed `agentbase.import.json` `orchestration` block matches the code: `orchestrator` equals `ORCHESTRATOR_ID`, `members` stays omitted (AgentBase discovers them), `defaultImport` is a supported mode, and the emitter's regex reader resolves the same id the runtime imports (drift guard for `pnpm emit:import-manifest`).                                                                                                                                                                                                                                                                                                                           | Passing                                      |

---

## Web Tests (Vitest, `apps/web/test/`)

| ID      | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Status                                                                                     |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| WEB-001 | `health.spec.ts` — shared `EnvSchema` defaults (`MASTRA_PORT`/`WEB_PORT`/`LOG_LEVEL`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Passing                                                                                    |
| WEB-002 | `a2a-only.spec.ts` — fitness guard: no web source references a non-A2A Mastra agent route (D-013)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Passing                                                                                    |
| WEB-003 | `docker-build.spec.ts` — fitness guard: every app Dockerfile builds via path filters (no rename-fragile `--filter @scope/…`) and asserts its artifact                                                                                                                                                                                                                                                                                                                                                                                                                             | Passing                                                                                    |
| WEB-004 | `logger.spec.ts` — web logger factory returns a usable logger, honors `LOG_LEVEL`, defaults to `info` on unset/invalid                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Passing                                                                                    |
| WEB-005 | `import-manifest.spec.ts` — `agentbase.import.json` `requiredEnv` matches the required set derived from the env schema (drift guard) + never gates reserved names                                                                                                                                                                                                                                                                                                                                                                                                                 | Passing                                                                                    |
| WEB-006 | `docker-compose-profiles.spec.ts` — every `pnpm docker:*` script routes through `scripts/docker-compose.mjs` (so root `.env` is read); agents/web/keycloak each declare a matching `profiles:` entry; web's `depends_on.agents` is `required: false`                                                                                                                                                                                                                                                                                                                              | Passing                                                                                    |
| WEB-007 | `agentbase-auth.spec.ts` — mints an AgentBase access token via OAuth2 `client_credentials` (correct `grant_type`/`client_id`/`client_secret`); caches + auto-refreshes before expiry; clear errors on missing config, non-2xx response, or a thrown request                                                                                                                                                                                                                                                                                                                       | Passing                                                                                    |
| WEB-008 | `a2a-client.spec.ts` — proxy mode POSTs the plain A2A `message/send` envelope (no wrapper) with the minted bearer to the agent's `AGENTBASE_AGENT_URL_<AGENT_ID>`; derives the env var name from the agent id; guard-rails on a missing per-agent URL or a token-mint failure (no fetch call in either case); direct mode unaffected (still `message/send` straight to Mastra); asserts the envelope carries `contextId` (→ Mastra `threadId`) **and** `message.metadata.resourceId`, which Mastra otherwise defaults to the agent id                                             | Passing                                                                                    |
| WEB-010 | `agent-context.spec.ts` — multi-tenancy guard for agent memory: `sanitizeConversationId()` strips the `::` namespace separator and all path characters (`../../other-user` → `other-user`), caps length at 64, returns `null` for unusable input; `buildThreadId()` always prefixes the server-owned `resourceId`, so a hostile conversation id (`::resource-b::conv-1`) still resolves inside the caller's own resource; route-handler source guard asserts it calls `resolveAgentContext()` and never reads `resourceId`/`threadId`/the legacy `sessionId` off the request body | Passing (17 cases)                                                                         |
| WEB-011 | `agentbase-discovery.spec.ts` — agent proxy URLs are DISCOVERED at runtime (AGENTDISC-1). The `.env.example` PLACEHOLDER counts as unconfigured — it is truthy, so a bare `!base` check let a fresh scaffold attempt DNS against a domain that does not exist; the placeholder is read from `.env.example` so the guard cannot drift from the shipped file: the Mastra id is read from each subscribed agent's A2A card (`card.name`), never from the listing's editable display name; an explicit `AGENTBASE_AGENT_URL_<AGENT_ID>` override short-circuits discovery with no network call; non-agent and REVOKED subscriptions are ignored. **Failure never degrades to a guessed URL** — missing `AGENTBASE_URL`, a failed `list_subscriptions`, an unknown id, and two agents claiming one id all return errors rather than a well-formed 404 URL. Caching: successes cached, failures NOT (a transient blip must not stick for a TTL), and concurrent callers share one in-flight discovery. Mutation-verified — falling back to a guess fails 2 cases, caching failures fails 1, picking arbitrarily on ambiguity fails 1, dropping single-flight fails 1 | Passing (12 cases) |
| WEB-009 | `typography.spec.ts` — `globals.css` never uses an invalid `text-wrap` value (`normal` is silently dropped by the parser; the opt-out is `wrap`), sets `pretty` on `body` so it inherits into Astryx-rendered `<span>` prose rather than enumerating tags, balances all six heading levels, and orders `components` after `astryx-theme`                                                                                                                                                                                                                                          | Passing (`apps/web/test/typography.spec.ts`, 4 cases; invalid-value guard negative-tested) |

---

## E2E UI Tests (Playwright, `apps/web/e2e/`)

> Autonomous — Playwright boots `next dev` itself. Verified on 2026-07-06 (Playwright 1.61.1, chromium). Run: `pnpm test:e2e`.

| ID      | Description                                                                        | Status  |
| ------- | ---------------------------------------------------------------------------------- | ------- |
| E2E-001 | Home page renders the `<h1>` app heading                                           | Passing |
| E2E-004 | `home.spec.ts` — an unknown route renders the app's OWN 404 (`not-found.tsx`), not Next's default page. `error.tsx` and `global-error.tsx` are covered by typecheck and review rather than e2e: forcing a render-time throw would mean shipping a route whose only purpose is to crash | Passing (1 case) |
| E2E-002 | Home page shows server health fetched from the Next.js route handler `/api/health` | Passing |
| E2E-003 | `GET /api/health` returns `{status:"ok"}` when hit directly                        | Passing |

---

## Tooling Tests

| ID       | Description                                                                                                                                                                                                                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                                         |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TOOL-001 | `rename-project.mjs` rewrites all functional `precast` refs, leaves docs prose                                                                                                                                                                                                                                                                                                                           | Passing                                                                                                                                                                                                                                                                        |
| TOOL-002 | `bootstrap.mjs` renames, resets git to a single commit on `develop`, activates hooks                                                                                                                                                                                                                                                                                                                     | Passing (verified in temp copy)                                                                                                                                                                                                                                                |
| TOOL-003 | `update-deps.mjs --dry` lists outdated packages without writing                                                                                                                                                                                                                                                                                                                                          | Passing                                                                                                                                                                                                                                                                        |
| APC-001  | The APC token source declares exactly the five shipped themes, and the generator, the `APC_THEME` constant, and `apc.tokens.json` agree on the ids                                                                                                                                                                                                                                                       | Passing (`apps/web/test/apc-theme.spec.ts`, 2 cases)                                                                                                                                                                                                                           |
| APC-002  | `apc-themes.css` is exactly what `pnpm theme:build` produces from the committed tokens (catches a hand-edited file or an un-regenerated token change), and every theme defines the core surface/accent roles                                                                                                                                                                                             | Passing (`apps/web/test/apc-theme.spec.ts`, 6 cases)                                                                                                                                                                                                                           |
| APC-003  | Each theme's selector list also targets Astryx's **nested** `[data-astryx-theme]` provider element — without it the tokens stop at `<html>` and never reach rendered content                                                                                                                                                                                                                             | Passing (`apps/web/test/apc-theme.spec.ts`, 5 cases) — regression: matching only `<html>` lost to the provider by inheritance proximity, silently ignoring every APC colour                                                                                                    |
| APC-004  | WCAG AA (4.5:1) for text on the primary action and for body text on the page background, in **both** light and dark, for all five themes                                                                                                                                                                                                                                                                 | Passing (`apps/web/test/apc-theme.spec.ts`, 10 cases) — regression: `--color-accent` flips from a dark brand colour (light) to a bright accent (dark), so a single `on-accent` role rendered white-on-lime at 1.3:1. Negative-tested: reintroducing the bug fails all 5 themes |
| TOOL-004 | `precast.manifest.json` is well-formed (semver `version`, repo URL, both ownership lists), references only paths that exist, never claims a path as both managed and advisory, keeps `package.json`/`.env.example`/`agentbase.import.json` advisory, and keeps the root configs managed                                                                                                                  | Passing (`apps/web/test/precast-sync.spec.ts`, 5 cases)                                                                                                                                                                                                                        |
| TOOL-005 | Manifest path matching handles exact entries and `dir/**` prefixes without sweeping in sibling directories (`scripts-old/` is not `scripts/**`)                                                                                                                                                                                                                                                          | Passing (`apps/web/test/precast-sync.spec.ts`, 2 cases)                                                                                                                                                                                                                        |
| TOOL-006 | `precast-update.mjs` 3-way classification is exact: `update` only when the project never touched the file, `yours` when upstream didn't move, `conflict` when both did, plus `new`/`restore`/`removed`/`unknown`/`current`                                                                                                                                                                               | Passing (`apps/web/test/precast-sync.spec.ts`, 8 cases)                                                                                                                                                                                                                        |
| TOOL-007 | `pnpm precast:update` end-to-end against a derived project: `--adopt` records a baseline; a check plan classifies a safe update, a new file, a conflict, a local customization and an advisory change correctly; `--apply` writes only the safe set and leaves a skipped conflict's baseline behind so it keeps reporting; `--force` takes upstream, writes a `.precast-bak`, and clears `pendingReview` | Passing (run live in a scratch copy renamed to a derived project, against a synthetic v0.2.0 upstream — all four stages verified)                                                                                                                                              |

---

## Documentation Contract Tests

| ID      | Description                                    | Status      |
| ------- | ---------------------------------------------- | ----------- |
| DOC-001 | CLAUDE.md references all required docs         | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| DOC-002 | HANDOFF.md §1, §2, §3, §4 all present          | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| DOC-003 | PROGRESS.md has all required sections (§0-§10) | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| DOC-004 | TECH_STACK.md has all required sections        | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| DOC-005 | ADRS.md has valid ADR format                   | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| DOC-006 | STYLE_GUIDE.md covers the topics CLAUDE.md defers to it for (TypeScript, Git, Validation, Logging, Error Handling, Testing) — **the original row did not name them**, and inventing a longer list would assert a contract nobody agreed to      | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
| DOC-007 | SCRIPTS.md covers all pnpm scripts             | Passing (`apps/web/test/repo-hygiene.spec.ts`) |
